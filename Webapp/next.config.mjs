/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

module.exports = {
    output: 'export',
    basePath: '/nextjs-app',
    assetPrefix: '/nextjs-app/',
  }
